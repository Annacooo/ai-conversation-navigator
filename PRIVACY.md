# Privacy

This extension runs on HTTPS pages so it can support multiple AI chat tools.

It only shows its toolbar on common AI chat sites or pages where a chat-like message structure is detected. It reads conversation text from the current page so it can jump between user questions and export selected content locally. It does not send conversation data to any server, does not make network requests, and does not collect analytics.
